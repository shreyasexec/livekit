# Frontend Environment Variables
# Use wss:// for secure WebSocket connection (required when page is served over HTTPS)
VITE_LIVEKIT_URL=wss://192.168.20.62:7880
# Empty or relative URL - API requests go through Vite proxy to avoid SSL cert issues
VITE_API_URL=
